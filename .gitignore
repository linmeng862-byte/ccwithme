node_modules/
data/
.env
.env.*
参考/
.codeflicker/
package-lock.json
*.log
.DS_Store
Thumbs.db
# Capacitor
ios/App/Pods/
ios/App/build/
android/
*.bak.*
backups/
.capacitor/
.playwright-mcp/

# 每台机器各自的路径，不共享
CLAUDE.local.md

# Atrio 会客厅：对外人格 prompt 是私人内容，ccwithme 是 PUBLIC 仓库，不进
# （仓库里只留上游的 *.example.md 作为结构参考；真正生效的这两份备份进 zxz）
atrio/prompts/system-prompt.md
atrio/prompts/memorize-prompt.md

# .bak 的漏网之鱼：原来只写了 *.bak.*（点号），
# 但实际文件叫 backend.js.bak-0822-0048（连字符），十个全没挡住，一直躺在 public 仓库里。
*.bak-*
*.bak

# 手表安装脚本（内嵌 base64 图标，400KB 的生成物，不进 git）
static/c.txt
